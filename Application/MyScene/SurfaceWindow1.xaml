<s:SurfaceWindow x:Class="MyScene.SurfaceWindow1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:s="http://schemas.microsoft.com/surface/2008"
    Title="MyScene"
    >

    <s:SurfaceScrollViewer HorizontalScrollBarVisibility="Auto">
        <s:ScatterView x:Name="scatter" 
                Height="2000" Width="2000" 
                BorderBrush="Black" BorderThickness="3">
            <s:ScatterViewItem CanScale="false" Height="1500" Width="1500" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="0" BorderBrush="Green" BorderThickness="10">
                <Image x:Name="background" Source="Resources/Images/Backgrounds/Scene 1.jpg" Stretch="Fill" /> <!--CJT may need to modify this a little, simulator makes it look uncentered and too large, but need to check on actual table -->
                
            </s:ScatterViewItem>
          <!--  <s:ScatterView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Image x:Name="img" Source="{Binding}"/>
                        <s:SurfaceButton Click="OnItemClicked"
                                     Content="Click Me!"
                                     Width="100" Height="80"/>
                    </Grid>
                </DataTemplate>
            </s:ScatterView.ItemTemplate> -->
        </s:ScatterView>
    </s:SurfaceScrollViewer>
</s:SurfaceWindow>
